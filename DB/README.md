# DB

SQL Server database planning area for schema scripts, seed data, indexes, and migration support.

* `Scripts/` will contain schema and operational SQL scripts in future tasks.
* `SeedData/` will contain safe non-production seed data in future tasks.

No database schema is implemented in this setup task.
